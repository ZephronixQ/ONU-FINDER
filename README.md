
# ONU Finder

**ONU Finder** — скрипт для поиска и сверки ONU по серийному номеру.  
Скрипт подключается к сетевым коммутаторам по Telnet, ищет указанный серийный номер и выводит таблицу с информацией о регистрации ONU:  

- Серийный номер  
- IP-адрес коммутатора  
- Порт, к которому привязан ONU  
- ID договора 

---


## Установка

Скрипт работает на Python 3.10+ и требует установки библиотеки:

```bash
pip install telnetlib3
````

Скрипт не имеет внешних зависимостей кроме стандартной библиотеки и `telnetlib3`.

---

## Настройки

Внутри `onu_finder.py` необходимо задать параметры подключения к коммутаторам:

```python
SWITCHES = [
    "192.168.2.12",
    "192.168.2.13",
    ....
]

USERNAME = "admin"
PASSWORD = "admin"
TELNET_PORT = 23
```

Вы можете изменить их под вашу сеть.

---

## Использование

Запуск скрипта осуществляется из терминала с передачей серийного номера ONU:

```bash
python3 onu_finder.py <ONU_SERIAL>
```

Пример:

```bash
python3 onu_finder.py 1234567890
```

### Пример вывода

```
🔍 Searching ONU serial: 1234567890

✅ ONU FOUND

┌───────────────┬───────────────┬─────────┬─────────┐
│  Switch IP    │    Serial     │  Port   │   ID    │
├───────────────┼───────────────┼─────────┼─────────┤
│ 192.168.2.12  │  1234567890   │ 1/1/1:1 │  ABCD12 │
└───────────────┴───────────────┴─────────┴─────────┘
```

* **Switch IP** — IP коммутатора (зелёный)
* **Serial** — серийный номер ONU (магента)
* **Port** — порт GPON ONU (белый)
* **ID** — идентификатор договора (жёлтый)

---

## Особенности

* Асинхронное подключение к нескольким коммутаторам для ускоренного поиска.
* Поддержка ANSI-кодов для корректного форматирования вывода.
* Таблица динамически подстраивается под длину данных.
* Простая интеграция в проект `diagnostics` и другие модули.
